# Secrets and local env
.env
.env.*
!.env.example

# Original resume PDFs (PII) — use config/resume_profile.md instead
*.pdf
Hardik_SWE_FullStack.pdf

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Node (if chosen later)
node_modules/

# OS / editor
.DS_Store
.idea/
.vscode/

# Runtime data (keep seen + quarantine tracked so Actions can persist them)
data/*
!data/seen_jobs.json
!data/quarantine.json
*.db

