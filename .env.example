# Copy this file to .env and paste your key:
#   cp .env.example .env
#
# Anthropic Console API key (Claude). Used later for the fit-matching step.
# Never commit .env — it is gitignored.
ANTHROPIC_API_KEY=
